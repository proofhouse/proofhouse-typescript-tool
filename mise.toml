# Local development runtime. mise no longer reads .node-version, so the
# pins live here. CI pins its own matrix, and the packageManager field in
# package.json keeps pnpm self-enforcing wherever the repository is used.
[tools]
node = "24.18.1"
pnpm = "11.25.0"
